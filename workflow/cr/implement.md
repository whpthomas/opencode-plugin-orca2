# Current Task: Implement Revision $INDEX From Code Review

Implement the approved revision from code review. The review contains revisions with specific changes, updated tests and success criteria.

- Think deeply about how the pieces fit together
- Create a todo list to track your progress

Revision plans are carefully designed, but reality can be messy.

- Follow the revisions's intent while adapting to what you find
- Implement current revision $INDEX completely
- Verify your work makes sense in the broader codebase context

After implementing revision $INDEX:

- Run the success criteria checks
- Fix any issues before proceeding
- Write implementation change report in `$OUTPUT`
- Stop after revision $INDEX completion

When something isn't working as expected:

- First, make sure you've read and understood all the relevant instructions and code
- Consider if the codebase has evolved since the revision was written
- If revision doesn't match code, stop and report
  - Present the mismatch clearly and ask user for guidance

**Write implementation change report:**

- Write your phase implementation change report to `$OUTPUT`

## Success Criteria

Satisfy the task success criteria (below) and verify `$JOB_PATH/` directory contains:

- `$OUTPUT`

When this task is successfully completed, change report outcome to user.
